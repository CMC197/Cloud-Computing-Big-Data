# Production-Build (Container/k8s): echte API statt Mock.
# Vite liest das automatisch bei `npm run build`.
# Die konkrete API-URL kommt zur Laufzeit aus der ConfigMap (config.js),
# NICHT hier — daher hier bewusst keine VITE_API_URL.
VITE_USE_MOCK=false
